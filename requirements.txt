# Ghost runtime dependencies
PySide6>=6.5        # Qt GUI (overlay, tray, SVG icons)
mss>=7.0            # screen capture
Pillow>=9.0         # image handling
ImageHash>=4.3      # screenshot change-detection
numpy>=1.22         # audio buffers
sounddevice>=0.4.5  # microphone / loopback capture
faster-whisper>=0.10  # local speech-to-text (offline fallback)
PyYAML>=6.0         # config + tools registry
# Groq cloud STT needs no package — just set GROQ_API_KEY (stdlib urllib).
